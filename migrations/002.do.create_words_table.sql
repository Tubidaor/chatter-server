CREATE TABLE chatter_words (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  words TEXT NOT NULL,
  date_created TIMESTAMP DEFAULT now() NOT NULL
);

ALTER TABLE chatter_words 
  ADD COLUMN
    child_id INTEGER REFERENCES chatter_child(id)
    ON DELETE SET NULL;